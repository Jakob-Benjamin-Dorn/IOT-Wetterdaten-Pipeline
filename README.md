# IOT-Wetterdaten-Pipeline

### Lokales Setup

Bis jetzt läuft das Projekt nur lokal.

### Anleitung

LocalStack wird lokal manuell gestartet:

export LOCALSTACK_AUTH_TOKEN=...
localstack start

Der Collector erwartet LocalStack unter:
http://localhost:4566