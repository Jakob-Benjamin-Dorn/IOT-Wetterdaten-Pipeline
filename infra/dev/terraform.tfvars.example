aws_region = "eu-central-1"

# Must be globally unique across all AWS accounts.
# Example:
# raw_bucket_name = "iot-wetterdaten-pipeline-dev-raw-yourname"
raw_bucket_name = "replace-with-globally-unique-bucket-name"
